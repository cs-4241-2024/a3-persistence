<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Home - Online To-Do List</title>
    <meta name="viewport" content ="width=device-width, initial-scale=1"/>
    <meta name="description" content="Online To-Do List" charset="utf-8" title="Online To-Do List"/>
    <script src="script.js"></script>
    <link rel="stylesheet" href="styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto+Mono:ital,wght@0,100..700;1,100..700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />


    <!-- get rid of favicon error -->
    <link rel="icon" href="data:;base64,iVBORw0KGgo=" />
  </head>
  <body>
    <header class="roboto-mono">
      <nav class="navbar bg-dark bg-body-tertiary ps-2" data-bs-theme="dark">
        <a class="navbar-brand" href="https://a3-aprilkat.glitch.me/"
          >Online To-Do List</a
        >
      </nav>
    </header>
    <main class="container mb-5">
      <div class="text-bg-warning container mt-3 rounded">
        <form>
          <div class="mb-3">
            <label for="newtask" aria-label="Enter a task" class="form-label pt-2" >Enter a task:</label>
            <input type="text" class="form-control" id="newtask" placeholder="task" value="task"/>
          </div>

          <div class="input-group mb-3">
            <label class="input-group-text" aria-label="Select a task priority" for="priority">Priority</label>

            <select class="form-select" id="priority">
              <option aria-label="High priority" value="High">High</option>
              <option aria-label="Medium priority" selected value="Medium">Medium</option>
              <option aria-label="Low priority" value="Low">Low</option>
            </select>
          </div>
          <button id="add" aria-label="Click to add the new task to the list" class="btn btn-dark mb-3">add</button>
        </form>
      </div>

      <div class="container mt-2 mb-5">
        <form id="tasklist"></form>
      </div>
    </main>

    <footer>
      <nav class="navbar bg-dark bg-body-tertiary" data-bs-theme="dark">
        <div class="container-fluid">
          <p id="footer-text" class="navbar-brand">
            April Bollinger &copy; 2024
          </p>
        </div>
      </nav>
    </footer>
  </body>
</html>
